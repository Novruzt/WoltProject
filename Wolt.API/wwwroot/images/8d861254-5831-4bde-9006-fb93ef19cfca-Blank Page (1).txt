 JwtConfig config = new JwtConfig();
            JwtService creator = new JwtService(config);

            RegisterUserResponseDTO respone = new RegisterUserResponseDTO()
            {
                Token = creator.CreateToken(user),
                Result = true
                
            };